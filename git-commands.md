
  Resumen:

  ✅ Repositorios configurados:
  - origin: git@github.com:dagorret/msp.git (original)
  - romanva: git@github.com:dagorret/Romanva.git (nuevo, público)

  ✅ Código subido a https://github.com/dagorret/Romanva

  Comandos útiles para el futuro:

  # Push a ambos repositorios
  git push origin main
  git push romanva main

  # O push a ambos a la vez
  git push origin main && git push romanva main

  # Ver logs
  git log --oneline -10

  # Hacer commit y push al nuevo repo
  git add .
  git commit -m "Descripción del cambio"
  git push romanva main


