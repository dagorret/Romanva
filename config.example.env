# Ejemplo de Configuración de Moodle Stats
# Copia este archivo y edita los valores según tu configuración

# ============================================
# CONFIGURACIÓN DE MOODLE (Base de Datos)
# ============================================

# Host donde está la base de datos de Moodle
# Puede ser: localhost, IP, o nombre de dominio
MOODLE_DB_HOST=localhost

# Puerto MySQL (usualmente 3306)
MOODLE_DB_PORT=3306

# Nombre de la base de datos de Moodle
MOODLE_DB_NAME=moodle

# Usuario de la base de datos
MOODLE_DB_USER=moodle_user

# Contraseña del usuario
MOODLE_DB_PASSWORD=tu_contraseña_aqui

# Prefijo de tablas de Moodle (usualmente mdl_)
MOODLE_DB_PREFIX=mdl_

# ============================================
# CONFIGURACIÓN DE DJANGO
# ============================================

# Secret key de Django (cambiar en producción)
# Genera una nueva con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-cambiar-en-produccion

# Modo debug (True para desarrollo, False para producción)
DEBUG=True

# Hosts permitidos (separados por coma)
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================
# NOTAS
# ============================================

# Para usar estas variables de entorno:
# 1. Cópialas a docker-compose.yml en la sección environment
# 2. O créalas en un archivo .env (no versionado)
# 3. O pásalas directamente al comando docker-compose up

# Ejemplo en docker-compose.yml:
# environment:
#   - MOODLE_DB_HOST=${MOODLE_DB_HOST}
#   - MOODLE_DB_PORT=${MOODLE_DB_PORT}
#   ...

# Ejemplo usando archivo .env:
# docker-compose --env-file .env up
